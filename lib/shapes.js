// Parent/superclass called `Shape` which has a constructor that initializes the `color` value
class Shape {
    constructor(color) {
        this.color = color;
    }
}

class Circle extends Shape {
    render() {
        return
    }
}